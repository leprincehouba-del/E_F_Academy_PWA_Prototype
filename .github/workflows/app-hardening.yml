name: App hardening patch

on:
  push:
    branches:
      - fix-data-integrity-sync-performance-20260909

permissions:
  contents: write

jobs:
  patch:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix-data-integrity-sync-performance-20260909
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply reviewed hardening changes
        run: python tools/app_hardening_patch.py
      - name: Capture app syntax report
        run: node --check app.js > .hardening-syntax.txt 2>&1 || true
      - name: Check service worker syntax
        run: node --check service-worker.js
      - name: Guard stale absolute points write
        run: "! grep -q 'points_balance: s.points' app.js"
      - name: Guard safe attendance RPC
        run: grep -q 'save_safe_attendance_with_account' app.js
      - name: Guard admin live sync
        run: grep -q 'refreshStudentSnapshotLive' app.js
      - name: Guard parent live sync
        run: grep -q 'refreshParentDashboardLive' app.js
      - name: Guard date-aware group time
        run: grep -q 'groupSessionStartTime' app.js
      - name: Guard cache version
        run: grep -q 'ef-academy-v43' service-worker.js
      - name: Commit hardening patch and syntax report
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app.js service-worker.js .hardening-syntax.txt
          git commit -m 'Harden points integrity, sync, and loading performance [hardening-bot]'
          git push origin HEAD:fix-data-integrity-sync-performance-20260909
